#pragma once

#include <vector>
#include <glm/glm.hpp>

class BernsteinBasisFunction {
    static double evaluate(double param, const std::vector<glm::vec3> coefficients) {
        auto n = coefficients.size();
        double result = 0;

        return result;
    }
};